import { CORE_MODULES } from './core';
import { INFRASTRUCTURE_MODULES } from './infrastructure';
import { PLATFORM_MODULES } from './platform';

export const MODULES = [...CORE_MODULES, ...INFRASTRUCTURE_MODULES, ...PLATFORM_MODULES];
